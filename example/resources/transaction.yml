name: 'transaction'

validation:

  create:
    type: 'object'
    required: true,
    properties:
      body:
        type: 'object'
        properties:
          value:
            required: true
          date:
            format: 'date'
