name: 'relation_guest'

pagination:
  default_page_size: 2

relationships:
  pets:
    resource: 'relation'
    cardinality: 'one-to-many'
    host: false

attributes:
  first_name:
    type: 'VARCHAR(30)'
    nullable: false
  last_name: 'VARCHAR(30)'

built_in_meta:
  created_at: false
  updated_at: false
