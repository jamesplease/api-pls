name: 'pizza'

relationships:
  toppings:
    resource: 'topping'
    cardinality: 'many-to-many'
    host: true

attributes:
  name: 'VARCHAR(30)'
  size: 'VARCHAR(30)'

built_in_meta:
  created_at: false
  updated_at: false
